import { RemoveSpaceFromClassPipe } from './remove-space-from-class.pipe';

describe('RemoveSpaceFromClassPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveSpaceFromClassPipe();
    expect(pipe).toBeTruthy();
  });
});
